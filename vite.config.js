import { defineConfig } from "vite";

export default defineConfig({
  // User pages (username.github.io) deploy at root; keep '/' by default.
  // If you deploy under a subpath, set base: '/<repo>/'
});
